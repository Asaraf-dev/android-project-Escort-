<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Escort" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/Voilet</item> <!-- menu img color -->
        <!-- Variant for darker shade -->
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="buttonIcon">@color/white</item>
        <item name="drawableTint">@color/white</item>
    </style>

    <style name="Theme.Escort" parent="Base.Theme.Escort" />

    <style name="Theme.Escort.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <style name="Theme.Escort.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Escort.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>